// The bare /chat route. ChatShell is rendered by the layout so it
// persists across navigations. This page exists only so Next.js
// matches the route; the layout handles all chat UI.
export default function ChatPage() {
  return null;
}
