// ChatShell is rendered by the chat layout, which also reads the
// threadId param and syncs it to the store. This page exists for
// route matching only — all chat UI is handled by the layout.
export default function ChatThreadPage() {
  return null;
}
